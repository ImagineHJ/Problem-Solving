#include <iostream>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL); cout.tie(NULL); //faster
    
    int n, m;
    cin>>n>>m;
    
    while(!(n==0 && m==0)){
        int arr[501]={0,};
        
        for(int i=0; i<m; i++){
            int a, b;
            
            cin>>a>>b;
            if(arr[a]==0 && arr[b]==0){
                
            }
            else if 
        }
        
        
    }
    
    return 0;
}

